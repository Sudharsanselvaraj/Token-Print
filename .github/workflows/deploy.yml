name: Deploy static site to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - run: npm ci

      - name: Fetch GitHub Contributors JSON
        run: |
          curl -s https://api.github.com/repos/${{ github.repository }}/contributors?per_page=100 > public/contributors.json || echo "[]" > public/contributors.json

      # Serve at the domain root (tokenprint.in) instead of /Token-Print.
      # NEXT_DISABLE_BASEPATH opts out of the basePath derived from
      # GITHUB_REPOSITORY (which GitHub pre-sets as a read-only override).
      - run: npm run build
        env:
          NEXT_DISABLE_BASEPATH: "1"

      - name: Skip deploy when Pages is disabled
        if: ${{ github.event.repository.has_pages != true }}
        run: echo "GitHub Pages is disabled for this repository; skipping deploy steps."

      # enablement:true turns Pages on (build_type=workflow) the first time this
      # runs, so a fresh clone/fork deploys without a manual Settings → Pages step.
      - uses: actions/configure-pages@v4
        if: ${{ github.event.repository.has_pages == true }}
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        if: ${{ github.event.repository.has_pages == true }}
        with:
          path: frontend/out

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        if: ${{ github.event.repository.has_pages == true }}
